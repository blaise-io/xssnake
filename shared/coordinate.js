'use strict';

/**
 * Coordinate.
 * Array with two values, x and y.
 *
 * @typedef {Array.<number>}
 * @example [5, 3]
 */
xss.Coordinate = new Array(2);

/**
 * Coordinate shift.
 * Array with two values, x and y.
 *
 * @typedef {Array.<number>}
 * @example [1, -2]
 */
xss.Shift;
