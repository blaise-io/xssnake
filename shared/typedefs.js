'use strict';

/**
 * Coordinate.
 * Array with two values, x and y.
 *
 * @typedef {Array.<number>}
 * @example [5, 3]
 */
xss.Coordinate;

/**
 * Coordinate shift.
 * Array with two values, x and y.
 *
 * @typedef {Array.<number>}
 * @example [1, -2]
 */
xss.Shift;
